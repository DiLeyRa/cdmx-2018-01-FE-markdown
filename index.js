const fs = require('fs');

fs.readFile('./README.md', data = (err,data) => {
    if(err){
        console.log(err);
    }
    text = data.toString();
    // console.log(text);

    for(let i=0; i<text.lenght; i++){
        let links = [];
        
    }
})